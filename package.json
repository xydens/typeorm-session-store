{
  "name": "typeorm-session-store",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "rm -rf ./dist && tsc && yarn run build:index",
    "lint": "tslint -p tsconfig.json -c tslint.json",
    "build:index": "rm -rf ./index.js ./index.d.ts && tsc -d --skipLibCheck --experimentalDecorators ./index.ts",
    "test": "jest",
    "coverage": "jest --coverage"
  },
  "files": [
    "dist/",
    "index.js",
    "index.d.ts"
  ],
  "peerDependencies": {
    "express": "^4.17.1",
    "express-session": "^1.16.1",
    "typeorm": "^0.2.17"
  },
  "dependencies": {},
  "devDependencies": {
    "express": "^4.17.1",
    "express-session": "^1.16.1",
    "typeorm": "^0.2.16",
    "@types/express": "^4.16.1",
    "@types/express-session": "^1.15.12"
  }
}
